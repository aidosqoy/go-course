# ДЗ 2.3: Дни недели с iota

## Цель
Освоить работу с константами и генератором iota.

## Что нужно сделать

1. Создать константы для дней недели (Понедельник = 1, ..., Воскресенье = 7)
2. Написать функцию `IsWeekend`, которая определяет, является ли день выходным

## Код для начала

```go
const (
    Monday = iota + 1  // = 1
    Tuesday            // = 2
    // ... продолжи
)

func IsWeekend(day int) bool {
    // твой код
}
```

## Пример использования

```go
func main() {
    fmt.Println(Monday)              // 1
    fmt.Println(Sunday)              // 7

    fmt.Println(IsWeekend(Saturday)) // true
    fmt.Println(IsWeekend(Wednesday)) // false
}
```

## Критерии выполнения

- [ ] Все 7 дней определены через iota
- [ ] Понедельник = 1, Воскресенье = 7
- [ ] Функция IsWeekend корректно определяет субботу и воскресенье
